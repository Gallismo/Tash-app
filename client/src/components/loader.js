import React from 'react';

export const Loader = () => {
    return (
    <div className="spinner-border text-primary loader text-center" role="status">
        <span className="sr-only">Loading...</span>
    </div>
    );
}